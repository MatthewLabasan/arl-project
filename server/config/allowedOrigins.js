const allowedOrigins = [
    'http://localhost:3000', // this is where our react app will be. therefore, only frontend can access our api/resources.
    'https://www.google.co.uk/'
]

module.exports = allowedOrigins